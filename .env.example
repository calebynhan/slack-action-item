# Bot token (xoxb-...) — OAuth & Permissions page of your Slack app
SLACK_BOT_TOKEN=xoxb-your-token

# App-level token (xapp-...) with connections:write — for Socket Mode
SLACK_APP_TOKEN=xapp-your-token
